name: Deploy Mobile App

on:
  workflow_dispatch:
    inputs:
      platform:
        description: '平台'
        required: true
        type: choice
        options:
          - both
          - ios
          - android
      build_type:
        description: '构建类型'
        required: true
        type: choice
        options:
          - staging
          - production

jobs:
  build-ios:
    if: ${{ github.event.inputs.platform == 'ios' || github.event.inputs.platform == 'both' }}
    runs-on: self-hosted
    timeout-minutes: 180
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - run: npm install --no-audit --no-fund

      - name: Build Next.js (verify only)
        run: npm run build

      - name: Sync Capacitor
        run: |
          npx cap sync ios
          sed -i '' 's/\.iOS(\.v1[789])/.iOS(.v16)/g' ios/App/CapApp-SPM/Package.swift

      - name: Cache SPM packages
        uses: actions/cache@v4
        with:
          path: |
            ~/Library/Developer/Xcode/DerivedData/*/SourcePackages
            ~/Library/Caches/org.swift.swiftpm
          key: spm-${{ runner.os }}-${{ hashFiles('ios/App/CapApp-SPM/Package.swift', 'ios/App/CapApp-SPM/Package.resolved') }}
          restore-keys: |
            spm-${{ runner.os }}-${{ hashFiles('ios/App/CapApp-SPM/Package.swift') }}
            spm-${{ runner.os }}-

      - name: Cache DerivedData
        uses: actions/cache@v4
        with:
          path: ~/Library/Developer/Xcode/DerivedData
          key: deriveddata-${{ runner.os }}-${{ hashFiles('ios/App/CapApp-SPM/Package.resolved') }}
          restore-keys: |
            deriveddata-${{ runner.os }}-

      - name: Set up Ruby (via mise)
        run: |
          eval "$(mise activate bash)"
          ruby --version
          cd ios && bundle install

      - name: Write App Store Connect API Key
        run: echo "${{ secrets.APPLE_API_KEY_P8 }}" > ios/fastlane/AuthKey_XX5239JSR5.p8

      - name: Diagnostic - check ios directory structure
        run: |
          echo "=== pwd ===" && pwd
          echo "=== ios/ ===" && ls -la ios/
          echo "=== ios/App/ ===" && ls -la ios/App/ || echo "ios/App/ does not exist!"
          echo "=== ios/App/App.xcodeproj ===" && ls -la ios/App/App.xcodeproj || echo "xcodeproj does not exist!"

      - name: Cleanup old development certificates
        run: |
          pip3 install --quiet --break-system-packages pyjwt cryptography requests
          python3 scripts/revoke_dev_certs.py
        env:
          APPLE_ISSUER_ID: ${{ secrets.APPLE_ISSUER_ID }}
          APPLE_KEY_PATH: ios/fastlane/AuthKey_XX5239JSR5.p8

      - name: Build and upload to TestFlight
        working-directory: ios
        run: |
          eval "$(mise activate bash)"
          xcodebuild -version
          bundle exec fastlane beta
        env:
          APPLE_ISSUER_ID: ${{ secrets.APPLE_ISSUER_ID }}
          APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
          DISTRIBUTION_CERT_P12: ${{ secrets.DISTRIBUTION_CERT_P12 }}
          DISTRIBUTION_CERT_PASSWORD: ${{ secrets.DISTRIBUTION_CERT_PASSWORD }}
          https_proxy: ""
          http_proxy: ""
          SSL_CERT_FILE: /etc/ssl/cert.pem

  build-android:
    if: ${{ github.event.inputs.platform == 'android' || github.event.inputs.platform == 'both' }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - run: npm install --no-audit --no-fund

      - name: Build Next.js (verify only)
        run: npm run build

      - name: Sync Capacitor
        run: |
          mkdir -p android/app/src/main/assets
          npx cap sync android

      - name: Set up Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.4'
          bundler-cache: true
          working-directory: android

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Decode Android keystore
        run: |
          echo "${{ secrets.ANDROID_KEYSTORE_FILE }}" | base64 -d > android/app/lingji-release.keystore
          cat > android/keystore.properties << PROPS
          storeFile=lingji-release.keystore
          storePassword=${{ secrets.ANDROID_KEYSTORE_PASSWORD }}
          keyAlias=${{ secrets.ANDROID_KEY_ALIAS }}
          keyPassword=${{ secrets.ANDROID_KEY_PASSWORD }}
          PROPS

      - name: Build and upload to Play Store
        working-directory: android
        run: bundle exec fastlane beta
        env:
          ANDROID_KEYSTORE_FILE: ${{ github.workspace }}/android/app/lingji-release.keystore
          ANDROID_KEYSTORE_PASSWORD: ${{ secrets.ANDROID_KEYSTORE_PASSWORD }}
          ANDROID_KEY_ALIAS: ${{ secrets.ANDROID_KEY_ALIAS }}
          ANDROID_KEY_PASSWORD: ${{ secrets.ANDROID_KEY_PASSWORD }}
          GOOGLE_PLAY_JSON_KEY_DATA: ${{ secrets.GOOGLE_PLAY_JSON_KEY_FILE }}
